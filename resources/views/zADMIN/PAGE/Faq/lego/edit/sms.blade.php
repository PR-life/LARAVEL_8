<div class="void / Form -S">
    {{-- @component('_wrap.__.summary')
        @slot('cssClose', 'Abs -lt')
        @slot('icoCloseName', 'closeRoll')
        @slot('id', 'wrapLegoParam')
        @slot('name', 'param')

        @slot('cssContent', 'space round-s / void-m')

        <div class="_shell _h1 / vol-Abs / net">
            <input id="inputCss" type="text" name="css" value="{{$lego->css ?? old('css')}}" placeholder=".">
            <label class="Abs" for="inputCss">@svg('plus')css</label>
        </div>

        <div>
            <div class="content Grey">Приоритет показа</div>
            <div class="paragraph"></div>
            @include('zADMIN.PAGE._lego.edit.param.where_to_show', ['Var' => $lego])
        </div>

        <div>
            <div class="content Grey">add_to_contents</div>
            <div class="paragraph"></div>
            @include('zADMIN.PAGE._lego.edit.param.add_to_contents', ['Var' => $lego])
        </div>

    @endcomponent --}}
</div>
