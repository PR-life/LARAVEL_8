<div class="Wings _indexMax / v-Seo -on">
    <div class="_win -t">
        <div class="space-left / _indexMax">
            <div class="space-left / flex cross-xxs / x-25101937">
        


                @component('zADMIN._wrap.manager.btn.localStorage_toggle')
                    @slot('localstorageName','filter_btn_table_title')
                    @slot('dataCheck','x-empty')
                    @slot('nodeName','Table_index')
                    @slot('toggleCss','-title')
                    @slot('name','title')
                @endcomponent
        
                {{-- @component('zADMIN._wrap.manager.btn.localStorage_toggle')
                    @slot('localstorageName','filter_btn_table_description')
                    @slot('nodeName','Table_index')
                    @slot('toggleCss','-description')
                    @slot('name','description')
                @endcomponent

                @component('zADMIN._wrap.manager.btn.localStorage_toggle')
                    @slot('localstorageName','filter_btn_table_tags')
                    @slot('nodeName','Table_index')
                    @slot('toggleCss','-tags')
                    @slot('name','tags')
                @endcomponent
 
                @component('zADMIN._wrap.manager.btn.localStorage_toggle')
                    @slot('localstorageName','filter_btn_table_category')
                    @slot('nodeName','Table_index')
                    @slot('toggleCss','-category')
                    @slot('name','category')
                @endcomponent --}}


            </div>
        
        </div>
    </div>
</div>
<div class="paragraphX2 / v-Seo -on"></div>

 